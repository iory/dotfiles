language:
  - python
notifications:
  email:
    recipients:
      - ab.ioryz@gmail.com
    on_success: always
    on_failure: always
sudo: required
env:
  matrix:
install:
  - sudo apt-get -qq update
  - sudo ./install.sh
  - sudo ./python-install.sh
script:
  - ipython -c 'import sys; sys.exit()'
